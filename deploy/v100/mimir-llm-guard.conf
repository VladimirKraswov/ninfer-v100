# This VM also has a legacy MIMIR llama.cpp service pulled in by mimir.target.
# Keep its archived configuration, but do not allocate a second GPU model while
# the NInfer production unit is installed. CPU ASR/TTS units are unaffected.
[Unit]
ConditionPathExists=!/etc/systemd/system/ninfer-v100.service
